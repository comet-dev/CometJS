var createInit = {
  init.create:"init.create();"
}
function init_tool(file){
  file.push("bin/comet");
  if(1 < 2){
    file.go(file); 
  }
}
var makeInit = new createInit;

init_tool(makeInit);
