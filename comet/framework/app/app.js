var app;

app.window.URL('1.100.COMET')&&app.window.open(app);

app.on("build");
/**
 * @overview Scaling apps to fit all different sizes.
 * @todo - Constant Bugging
 **/
app.use(window.open(app));

