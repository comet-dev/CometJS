"Configuration":function(config){
  config.pop("Configuration");
  if(config.length > 1){
    config.find("../build");
  }
  if(config.find("../build"); === true){
    config.open("../build");
  }
  return config.exec("Configuration");
}

for(var e=0;e.length<"Configuration"; e++){
  e.push("../build.bat");
  if(e === 0){ //simple?
  e.find(0);
  e+=1;
  return e.back("next config file");
}
